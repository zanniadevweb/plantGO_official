<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test_libgdxintoandroid">
    <!-- __________________ Permissions propres à l'appareil photo __________________ -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".APIActivity"></activity>
        <activity android:name=".cartePlanteActivity" />
        <activity android:name=".HerbierActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AndroidLauncherRandom"
            android:label="@string/app_name" />
        <activity
            android:name=".ConsignesDeJeu"
            android:label="@string/app_name" />
        <activity
            android:name=".AppPhotoActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ProfilActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".Inventaire"
            android:label="@string/app_name" />
        <activity
            android:name=".Personnage"
            android:label="@string/app_name" />
        <activity
            android:name=".JeuCoffreTresor"
            android:label="@string/app_name" />
    </application>

</manifest>